#set up properties
PROJECT_ID: qwiklabs-gcp-02-be2c661648ac

server:
    port: ${DEFAULT_PORT:80}

spring:
    data:
        rest: 
            base-path: /api
    cloud:
        config:
            namespace: default
            enabled: true
            name: Cloud
            profile: dev, cloud, firestore
            label: master
        gcp:
            credentials:
                location: classpath:key.json  
            firestore:
                namespace: ${DATASTORE_NAME:marketplace}
                project-id: ${PROJECT_ID:qwiklabs-gcp-02-be2c661648ac}
                enabled: true